# Email Configuration for Contact Form
# Gmail SMTP Settings (use your personal Gmail account)

# Your Gmail email address (both sender and recipient)
GMAIL_USER=your-email@gmail.com

# Gmail App Password (NOT your regular password)
# Generate this from: Google Account > Security > 2-Step Verification > App passwords
GMAIL_APP_PASSWORD=your-16-character-app-password

# Gmail SMTP Configuration (usually don't need to change these)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Application URL (for email context)
APP_URL=http://localhost:3000
